# @package _global_
# Kaggle score: 0.127735
defaults:
  - override /model: graph_cent

model:
  gcn_type: traditional

training:
  epochs: 50
  loss: mixed
  seeds: [42]
  ensemble: true
  aug_noise: 0
  aug_edge_drop: 0
  plots: false
