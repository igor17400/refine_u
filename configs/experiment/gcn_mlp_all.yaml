# @package _global_
# GCN + MLP with all features (eigen + betweenness)
defaults:
  - override /model: graph_cent

model:
  gcn_type: traditional
  feat_type: both

training:
  epochs: 50
  loss: mixed
  seeds: [42]
  ensemble: true
  aug_noise: 0
  aug_edge_drop: 0
